# field-story-scorer
CLI tool that scores Excel columns across five data quality dimensions and outputs ranked Excel and PDF reports. Includes strict cell-level type detection that catches mixed-type columns pandas silently coerces.
